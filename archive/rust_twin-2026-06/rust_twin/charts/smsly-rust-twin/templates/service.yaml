apiVersion: v1
kind: Service
metadata:
  name: {{ include "smsly-rust-twin.fullname" . }}-worker
  labels:
    {{- include "smsly-rust-twin.componentLabels" (dict "root" . "component" "worker") | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    {{- include "smsly-rust-twin.componentSelectorLabels" (dict "root" . "component" "worker") | nindent 4 }}
