import asyncio
from unittest.mock import MagicMock
import json
from backend.apps.deployments.consumers import TerminalConsumer

# Simulate a test to see if TerminalConsumer is breaking
print("TerminalConsumer exists")
