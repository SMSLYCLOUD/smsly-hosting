# Prometheus Configuration for SMSLY Hosting
# Scrapes metrics from services

global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: 'smsly-hosting'
    environment: 'production'

# Alertmanager configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets: []
          # - alertmanager:9093

# Rule files
rule_files: []
  # - /etc/prometheus/rules/*.yml

scrape_configs:
  # Prometheus itself
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # SMSLY Backend (Django)
  - job_name: 'smsly-backend'
    static_configs:
      - targets: ['smsly-backend:8000']
    metrics_path: /metrics
    scrape_interval: 30s

  # PostgreSQL
  - job_name: 'postgres'
    static_configs:
      - targets: ['smsly-postgres:5432']
    # Uses postgres_exporter

  # Redis
  - job_name: 'redis'
    static_configs:
      - targets: ['smsly-redis:6379']
    # Uses redis_exporter

  # Docker containers using cAdvisor
  - job_name: 'cadvisor'
    static_configs:
      - targets: ['cadvisor:8080']

  # Node metrics
  - job_name: 'node'
    static_configs:
      - targets: ['node-exporter:9100']

  # Nginx
  - job_name: 'nginx'
    static_configs:
      - targets: ['smsly-nginx:9113']
    # Uses nginx_prometheus_exporter
