fix: prevent install scripts from aborting installer and skip Kata when gVisor present

- Wrapped install-gvisor.sh and install-kata.sh in main() with
  BASH_SOURCE guard so they're safe to source (install.sh lib loop)
  without terminating the process
- Added both to install.sh's lib skip pattern
- Added early return in harden.sh: if gVisor is/was installed, skip Kata
  entirely — no point running two sandboxing runtimes

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
