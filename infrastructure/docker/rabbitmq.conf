# SMSLY RabbitMQ overrides — loaded from /etc/rabbitmq/conf.d/
# Soft memory limit: pause publishers at 60% of detected RAM until
# consumers drain the backlog.  Docker memory limit (1G) is the
# hard safety net and should never be hit at normal load.
vm_memory_high_watermark.relative = 0.6
